# دليل إعداد وربط OpenAI API

## 🎯 نظرة عامة

المشروع يستخدم OpenAI API في ثلاث وظائف رئيسية:
1. **فحص محتوى التقييمات** (Moderation) - فحص النصوص والصور
2. **تحليل البيانات** (AI Insights) - توصيات ذكية للـ Dashboard
3. **فلترة الصور** (Vision API) - فحص محتوى الصور

---

## 📋 المتطلبات

1. حساب OpenAI (يمكن إنشاءه من https://platform.openai.com)
2. OpenAI API Key
3. رصيد في حساب OpenAI (للبدء يمكن استخدام الرصيد المجاني)

---

## 🔑 الخطوة 1: الحصول على OpenAI API Key

### أ) إنشاء حساب OpenAI

1. اذهب إلى: https://platform.openai.com/signup
2. سجّل حساب جديد (يمكن استخدام Google/Microsoft)
3. أكمل عملية التحقق

### ب) إنشاء API Key

1. بعد تسجيل الدخول، اذهب إلى: https://platform.openai.com/api-keys
2. اضغط "Create new secret key"
3. أعطه اسم (مثلاً: `theqah-production`)
4. **انسخ الرمز فوراً** - لن تظهر مرة أخرى!
   - مثال: `sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### ج) إضافة رصيد (مهم!)

1. اذهب إلى: https://platform.openai.com/account/billing
2. اضغط "Add payment method"
3. أضف بطاقة دفع (يوجد رصيد مجاني $5 للبدء)

**ملاحظة:** بدون رصيد، API لن يعمل!

---

## 🔧 الخطوة 2: إضافة API Key إلى Vercel (للإنتاج)

### أ) من لوحة تحكم Vercel

1. اذهب إلى: https://vercel.com/dashboard
2. اختر مشروع `theqah`
3. اذهب إلى Settings → Environment Variables
4. أضف متغير جديد:
   - **Name:** `OPENAI_API_KEY`
   - **Value:** `sk-proj-...` (الرمز الذي نسخته)
   - **Environments:** اختر Production, Preview, Development (كلها)
5. اضغط "Save"

### ب) (اختياري) إضافة Model Config

إذا أردت تغيير الـ model الافتراضي:
- **Name:** `OPENAI_MODEL`
- **Value:** `gpt-4o-mini` (أو `gpt-4o`, `gpt-4-turbo`)
- **Environments:** Production, Preview, Development

### ج) Redeploy

بعد إضافة المتغيرات:
1. اذهب إلى Deployments
2. اختر آخر deployment
3. اضغط "..." → "Redeploy"
4. تأكد من اختيار "Use existing Build Cache" = Off (لإعادة بناء مع المتغيرات الجديدة)

---

## 💻 الخطوة 3: إضافة API Key محلياً (للاختبار)

### أ) إنشاء ملف `.env.local`

في المجلد الرئيسي للمشروع (`D:\WEB\theqah`):

```bash
# إنشاء ملف .env.local
touch .env.local
```

أو من Windows:
```powershell
New-Item .env.local -ItemType File
```

### ب) إضافة المتغيرات

افتح `.env.local` وأضف:

```bash
# OpenAI API Key (مطلوب)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI Model (اختياري - الافتراضي: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
```

**⚠️ مهم:** لا ترفع `.env.local` على GitHub! (موجود في `.gitignore`)

### ج) إعادة تشغيل Server

```bash
# أوقف السيرفر (Ctrl+C)
# ثم شغله مرة أخرى
npm run dev
```

---

## ✅ الخطوة 4: التحقق من أن AI يعمل

### أ) اختبار Moderation (فحص التقييمات)

1. افتح المشروع: http://localhost:3000
2. اذهب إلى صفحة تقييم
3. حاول إرسال تقييم
4. تحقق من:
   - إذا كان التقييم مقبول → يُنشر مباشرة
   - إذا كان يحتوي على كلمات مسيئة → يُرفض

### ب) اختبار AI Insights (لوحة التحكم)

1. سجّل دخول إلى Dashboard
2. اذهب إلى صفحة Analytics
3. يجب أن ترى قسم "توصيات الذكاء الاصطناعي"
4. بعد تحميل البيانات، يجب أن تظهر توصيات بالعربية

### ج) اختبار من Terminal (متقدم)

```bash
# اختبار Moderation API
curl -X POST http://localhost:3000/api/reviews/submit \
  -H "Content-Type: application/json" \
  -d '{
    "orderId": "TEST-001",
    "stars": 5,
    "text": "منتج رائع!",
    "tokenId": "..."
  }'
```

---

## 🔍 حل المشاكل

### مشكلة 1: "OPENAI_API_KEY is required"

**السبب:** الـ API Key غير موجود أو غير صحيح

**الحل:**
1. تحقق من وجود `.env.local` في المجلد الرئيسي
2. تأكد من أن المفتاح يبدأ بـ `sk-`
3. تأكد من عدم وجود مسافات قبل/بعد المفتاح
4. أعد تشغيل السيرفر

### مشكلة 2: "Insufficient quota" أو "You exceeded your current quota"

**السبب:** انتهى الرصيد في حساب OpenAI

**الحل:**
1. اذهب إلى: https://platform.openai.com/account/billing
2. تحقق من الرصيد
3. أضف رصيد جديد إذا لزم الأمر

### مشكلة 3: "Invalid API key"

**السبب:** المفتاح غير صحيح أو منتهي الصلاحية

**الحل:**
1. أنشئ مفتاح جديد من: https://platform.openai.com/api-keys
2. حدّث `.env.local` و Vercel Environment Variables

### مشكلة 4: "Rate limit exceeded"

**السبب:** تجاوزت الحد المسموح من الطلبات

**الحل:**
- انتظر قليلاً ثم حاول مرة أخرى
- أو ارفع الحد من: https://platform.openai.com/account/limits

---

## 📊 التكلفة المتوقعة

### تكلفة Moderation (فحص التقييمات)

| العملية | التكلفة التقريبية |
|---------|-------------------|
| فحص نص واحد | ~$0.00001 |
| فحص صورة واحدة | ~$0.0001 |
| **تكلفة تقييم واحد** | **~$0.00001 - $0.0005** |

**مثال:**
- 1,000 تقييم بدون صور = ~$0.01
- 1,000 تقييم مع صور (4 صور لكل) = ~$0.50

### تكلفة AI Insights

| العملية | التكلفة التقريبية |
|---------|-------------------|
| توليد توصيات واحدة | ~$0.00004 |

**مثال:**
- 1,000 توصية = ~$0.04

### التكلفة الشهرية المتوقعة

لـ 10,000 تقييم/شهر:
- Moderation: ~$1-5
- AI Insights: ~$0.40
- **الإجمالي: ~$1.5-5.5/شهر**

---

## 🔐 الأمان

### ✅ Best Practices

1. **لا ترفع `.env.local` على GitHub**
   - ✅ موجود في `.gitignore`
   - ✅ لا ترفعه يدوياً

2. **استخدم Environment Variables في Vercel**
   - ✅ آمن ومشفّر
   - ✅ لا يظهر في الكود

3. **راقب الاستخدام**
   - تحقق من: https://platform.openai.com/usage
   - ضع حدود للإنفاق: https://platform.openai.com/account/billing/limits

4. **استخدم MCP (Model Context Protocol) للتطوير**
   - يمكن إعداد OpenAI عبر MCP للاختبار

---

## 📝 ملخص سريع

### للتطوير المحلي:
```bash
# 1. أنشئ .env.local
echo "OPENAI_API_KEY=sk-proj-..." > .env.local

# 2. شغّل المشروع
npm run dev
```

### للإنتاج (Vercel):
1. اذهب إلى Vercel → Settings → Environment Variables
2. أضف `OPENAI_API_KEY` = `sk-proj-...`
3. Redeploy

---

## 🎯 الخطوات السريعة

### ✅ للبدء الآن:

1. **احصل على API Key:**
   - https://platform.openai.com/api-keys
   - أنشئ مفتاح جديد وانسخه

2. **أضفه محلياً:**
   ```bash
   # في D:\WEB\theqah
   echo OPENAI_API_KEY=sk-proj-YOUR_KEY_HERE > .env.local
   ```

3. **أضفه على Vercel:**
   - Vercel Dashboard → Settings → Environment Variables
   - أضف `OPENAI_API_KEY`

4. **اختبر:**
   - شغّل `npm run dev`
   - افتح Dashboard وتحقق من AI Insights

---

## 📚 روابط مفيدة

- OpenAI Platform: https://platform.openai.com
- API Keys: https://platform.openai.com/api-keys
- Usage & Billing: https://platform.openai.com/usage
- Documentation: https://platform.openai.com/docs
- Pricing: https://openai.com/api/pricing

---

## ✅ الخلاصة

1. ✅ احصل على OpenAI API Key
2. ✅ أضفه إلى `.env.local` (للاختبار)
3. ✅ أضفه إلى Vercel Environment Variables (للإنتاج)
4. ✅ أضف رصيد في حساب OpenAI
5. ✅ اختبر من Dashboard

**بعد ذلك، AI سيعمل تلقائياً في:**
- فحص التقييمات (Moderation)
- توصيات Dashboard (AI Insights)
- فحص الصور (Vision API)

🚀 **جاهز للاستخدام!**

